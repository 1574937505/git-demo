# 开发环境配置
spring:
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/soul_app?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root

  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0

# JWT配置
jwt:
  secret: soul-app-dev-secret-key-please-change-in-production-environment-2024
  expiration: 86400000
  refresh-expiration: 604800000

# 日志配置
logging:
  level:
    root: info
    com.soul: debug
    com.soul.mapper: debug
